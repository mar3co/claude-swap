# Implementation Plans

Generated by the improve skill on 2026-09-05. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

Repo: `/Users/yohan/GitHub/openswap` (mar3co fork). Tests: `uv run pytest`.
Commit style: `feat(menubar): …` / `fix(…): …` / `docs: …`. Origin is
`https://github.com/mar3co/openswap.git`. Do not push to `upstream`
(`realiti4/claude-swap`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Show org names on extra, title, and widget | P1 | S | — | DONE |
| 006  | Refuse PyPI upgrade for an editable git checkout | P1 | S | — | DONE |
| 002  | Add soonest-5h strategy and a hold-reason line | P1 | M | 001 | DONE |
| 003  | Show live Claude Code sessions after a switch | P1 | S | 001 | DONE |
| 004  | Move Settings into the popover | P2 | M | 001, 003 | DONE |
| 005  | Widget tap-to-switch and accessory families | P2 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

001 and 006 share no files and may run in parallel. 002–005 all touch the extra
and/or widget; run them only after 001 is merged into the executor’s base.

## Dependency notes

- 002 uses 001’s org titles in the hold-reason line (`personal` vs `Ads Online`).
- 003 adds a footer/status line next to 001’s subtitle row; do not start until
  card height math in `menubar_panel.py` is the post-001 version.
- 004 rebuilds the popover chrome (footer buttons, a settings page). It must
  see 001’s subtitle and 003’s running-sessions line.
- 005’s widget UI shows `AccountCard.subtitle`; that field is filled correctly
  only after 001.

## Findings considered and rejected

- Chrome / claude.ai cookie sync (upstream #256): large and security-sensitive.
- Rewriting the rumps extra in Swift: the popover already works; WidgetKit is
  the part that had to be Swift.
- GitHub Pages as a second user site: already decided against.
- Auto-restarting Claude Code after a switch: easy to get wrong; 003 only
  displays liveness.
- Publishing a separate PyPI package name: premature. 006 only guards upgrade.
- WidgetKit `accessoryCircular` / `accessoryRectangular` on this macOS target:
  `@available(macOS, unavailable)` on the current SDK. 005 shipped tap-to-switch
  and documented the family skip.

## Execution notes

001 and 006 were reviewed and merged first. 002–005 ran sequentially on top of
that base. All six are on `feat/menubar-usage-bars` as of `5d8dcb1` plus this
plans index.
